include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude: [build/**, lib/**.freezed.dart, lib/**.g.dart, test/**.mocks.dart, 
  packages/**.freezed.dart, packages/**.g.dart, packages/**.mocks.dart]
  
linter:
  rules:
    public_member_api_docs: false
    avoid_print: false